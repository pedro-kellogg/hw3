<h1><%= @place.name %></h1>

<% @place.entries.each do |entry| %>
  <div>
    <h3><%= entry.title %></h3>
    <p><%= entry.occurred_on %></p>
    <p><%= entry.description %></p>
  </div>
<% end %>

<a href="/places/<%= @place.id %>/entries/new">New Post</a>

<a href="/places">Back to Places</a>